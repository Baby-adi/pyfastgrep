[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "pyfastgrep"
version = "0.1.0"
description = "Fast code search using ripgrep engine from Python"
authors = [{ name = "Your Name" }]
requires-python = ">=3.8"

[tool.maturin]
python-source = "python"
